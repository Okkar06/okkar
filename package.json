{
  "name": "okkar-profolio",
  "version": "1.0.0",
  "scripts": {
    "render-build": "cd client && npm install && npm run build && cd ../server && npm install && npx prisma generate && npx prisma migrate deploy",
    "start": "node server/src/server.js"
  }
}
